// Reuse the landing page located in the (landing-page) route so `/` shows the new landing content.
// We import and re-export the default component from that route.

import LandingPage from './(landing-page)/page';

export default LandingPage;
